<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bayu.cvsuryakencana">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MenuUtamaActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".DataProductActivity" />
        <activity android:name=".DataDistributorActivity" />
        <activity android:name=".DataUserActivity" />
        <activity android:name=".AddProductActivity" />
        <activity android:name=".UpdateProductActivity" />
        <activity android:name=".AddDistributorActivity" />
        <activity android:name=".UpdateDistributorActivity" />
        <activity android:name=".UpdateUserActivity"></activity>
    </application>

</manifest>
